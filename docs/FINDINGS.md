# Findings → countermeasures → effectiveness

Source: the owner's own security investigation on the owner's LG webOS TV, webOS 10.3.1.

| ID | Finding | Module | Effectiveness | Honest note |
|----|---------|--------|---------------|-------------|
| F1  | ACR (automatic content recognition) running in background, marking viewing | `services` (acr) | **Full** | `systemctl stop acr` now + stop again on every boot via the `oyg` init.d hook. Mask is impossible on this device (see F14a). |
| F2  | Alphonso / ACR ad overlay stack (adoverlay) | `services` (adoverlay) | **Full** | stopped + boot-enforced; survives reboot via the init.d hook reading `services.stopped`. |
| F5  | Voice → plaintext transcripts in `/tmp/var/log/messages` | `voice` (kill pipeline) + `logs` (clear damage) | **Full** | Magic Remote mic pipeline (`voiceinput_hidraw` → `voiceinput` → `voiceconductor`) is neutralised by the `voice` module: `chmod 000` then `mount --bind /dev/null` over each binary, then TERM+KILL any running instance. The mic button continues to register as a HID key event but no capture and no transcription occur (verified: `/tmp/app.voice.log` stayed at 0 bytes across two mic-button presses; `/tmp/var/log/messages` recorded only `lginput2 NL_BUTTON_CLICK {"remote_type":"LGE MR25GA","button_type":"KEY_VOICE"}`; zero `user_utterance`, zero voice `NL_*` events; no respawn after 10 s). The `logs` module continues to clear the plaintext log files every `${OYG_LOG_INTERVAL:-60}s` as belt-and-braces damage limitation, but it is no longer the only defence. |
| F8  | Mic / audio-bus capture by vendor processes | `mic`, `services` (objectdetection) | **Full — capture device nodes neutralised** | The previous `amixer`-based approach was a dead end on this device: the writable capture control is `numid=628` (`Adc Open`) and the driver returns `Operation not permitted` on close, so the module could only ever report `PARTIAL`. The current approach cuts the pipe at the device-node level — `chmod 000` then `mount --bind /dev/null <node>` over every ALSA capture PCM (pcmC*D*c). Capture nodes are enumerated dynamically (walk `/dev/snd/pcmC*` + cross-check `/proc/asound/pcm` lines containing `capture`); playback nodes (pcmC*D*p) are deliberately untouched. Verified on device: `arecord -D hw:0,10 ... | wc -c` goes from 64000 bytes to 0, with `arecord` failing to open ("Inappropriate ioctl for device"); `paplay -d pcm_output <file>` still returns rc=0 (playback preserved). Status actively verifies with `arecord` when available. Caveat: this stops the ALSA capture path; it does not stop a kernel-level actor that bypasses the bind. Bind-mounts do not survive a reboot — the boot hook re-applies the `mic` module on every boot. Disabling `objectdetection` cuts the consumer of that audio. |
| F9  | `/tmp/capture.rgb` (640×360 RGB24) written every ~3s by `com.webos.service.oledepl` | `capture` | **Full** | `chattr +i` if supported; otherwise `chmod 000` + watcher re-applying mode (the writer re-creates the file). |
| F19 | `/tmp/capture.rgb` readable inside app sandboxes | `capture` | **Full** (once chmod 000) | App jails cannot stat past mode 0. |
| F10 | Video-plane capture via `vtCaptureTestSuite` | `capture` | **Full** | Strategy ladder: (1) `mount --bind /dev/null <path>` (verified on this device — works even though `/usr` is read-only), (2) `chmod 000` if the path is writable, (3) FAIL with reason. The bind-mount replaces the binary with the null device for execution purposes; `restore` umounts the exact path recorded in state. |
| F11 | Telnet + default SSH password | (out of scope) | **Not possible** | We do not ship a patcher for the default password. Rotate the SSH password manually; this is the owner's responsibility. |
| F12 | Unauthenticated Chromium DevTools on port 9998 | (out of scope) | **Not possible** | Port 9998 is opened by the vendor Chromium; killing it requires touching the Chromium launcher and is a per-update fix. Not handled by `oyg`. |
| F14 | Telemetry beacons to `ngfts.lge.com` etc. | `network` (layers 1+2+3) | **Layer 1 full for libc consumers; layer 2 partial; layer 3 ages as CDNs rotate** | Layer 1: bind-mount `/var/lib/own-your-glass/hosts` over `/etc/hosts`. Layer 2: blackhole hardcoded public resolvers (`8.8.8.8`, `1.1.1.1`, `9.9.9.9`, …). Layer 3 (opt-in `OYG_NETWORK_IPBLOCK=1`): `ip route add blackhole` per resolved IP. **DoH/443 and DoT/853 cannot be blocked** — netfilter is unavailable. CDN IPs change; refresh the list. The upstream `lg-tv-blocklist` repo's `dns-egress` hook is iptables-based and **does not work on this device** (`ip_tables` absent) — worth reporting upstream. |
| F16 | World-writable, root-executed webOSbrew hbchannel service code | `perms` | **Full** (when `OYG_AGGRESSIVE=1`) | chmod 0755 on dirs, 0644 on files. Will likely break webOSbrew updates — keep an eye. Re-applied on every boot when `perms.applied=1` (see boot hook) because webOSbrew updates may re-extract these paths in mode 0777 between boots. |
| F17 | World-writable `com.webos.ghp.runtime/lib` and `/conf` | `perms` | **Full** (when `OYG_AGGRESSIVE=1`) | Same. May break Google Home on the TV. Also boot-enforced. |
| F18 | RemoteOne remote support gated by `/mnt/lg/cmn_data/remoteDebug/` | `remoteone` | **Partial** | We verify absence and report loudly if present. We do NOT delete the gate — that decision belongs to the owner. If absent we confirm hardened state. |
| F30 | Same gate but emphasis on the world-writable parent dir | `remoteone` | **Full** (when `OYG_AGGRESSIVE=1`) | `/mnt/lg/cmn_data` is 0777; aggressive mode chmods to 0755. |
| F20 | `contentminer` service | `services` | **Full** | stopped + boot-enforced (see F14a). |
| F22 | `objectdetection` service | `services` | **Full** | stopped + boot-enforced (see F14a). |
| F25 | Ad-tech cookies | (mitigated indirectly) | **Partial** | We stop the ad-services, block their telemetry hosts, and stop the overlay renderer. We do not edit Chromium cookie stores. |
| F26 | Cleartext IFA (ID for Advertisers) | `network` (layer 1 + 3) | **Partial** | Layer 1 maps `demdex.net`, `doubleclick.net`, `everesttech.net`, `criteo.com`, `dpm.demdex.net`, `sxp.smartclip.net`, `hbbtv.smartclip.net` to `0.0.0.0` for libc consumers. Layer 3 adds blackhole routes for any daemons that resolve-then-cache at startup. Other IFA endpoints not in the list — add to `etc/blocklist-oyg.txt`. |
| F14a | `/etc/hosts` is on a read-only overlay on this device; `systemctl mask` is therefore impossible | `network` (layer 1 workaround); `services` (enforcement model) | **Full** for layer 1; **Full** for service stoppage | `/etc` is RO, but `mount --bind /var/lib/own-your-glass/hosts /etc/hosts` works (verified). `getent hosts` honours it. `nslookup` and BusyBox's own resolver bypass it (verified). `systemctl mask` is unimplementable here (every attempt fails with `Failed to mask unit: File /etc/systemd/system/<x>.service already exists.`). The `services` module therefore enforces "stop unit + kill process, repeat on every boot" — `systemctl stop` works for systemd-managed services (verified), and `kill` works for luna-launched ones (see F33a). Unit names are recorded in `/var/lib/own-your-glass/services.stopped` and process names in `/var/lib/own-your-glass/services.kill`; the boot hook re-applies both on every boot. Status reports `unit stopped + process killed, boot-enforced`, never `masked`. |
| F14b | Some webOS daemons hardcode public resolver IPs and bypass `/etc/hosts` | `network` (layer 2) | **Partial** | `ip route add blackhole` for `8.8.8.8`, `8.8.4.4`, `1.1.1.1`, `1.0.0.1`, `9.9.9.9`, `208.67.222.222`, `208.67.220.220` (verified blackhole-able). **DoH over TCP/443 and DoT over TCP/853 cannot be intercepted** — netfilter unavailable. Optional `OYG_DNS_OVERRIDE=1` rewrites `/var/lib/misc/resolv.conf` with the default gateway as nameserver. |
| F14c | Upstream `lg-tv-blocklist`'s `dns-egress` hook is iptables-based | `network` (workaround) | **N/A** | The upstream hook **does not work on this device** — `ip_tables` kernel module absent. The bind-mount approach used here is the device-compatible replacement. Worth reporting upstream. |
| F14d | Vendored upstream SAFE blocklist (CC BY 4.0) | `network` (layer 1 + 3) | **Full** for what the upstream proved | `etc/blocklist-upstream-safe.txt` is a 2026-09-14 snapshot of [furkan-bayrak/lg-tv-blocklist](https://github.com/furkan-bayrak/lg-tv-blocklist) (21 entries, attribution preserved in the file header). Refresh with `scripts/refresh-blocklist.sh` (off-device only). |
| F14f | IPv4-only `/etc/hosts` sinkhole is bypassed by AAAA lookups (hosts file has no IPv6 answer, resolver falls through to DNS) | `network` (layer 1) | **Full** with both stacks, **Partial** (effectively bypassed) with IPv4 only | Verified live on device: `modules/network.sh` previously emitted only `0.0.0.0 <domain>` lines, so `getent hosts criteo.com` and `getent hosts doubleclick.net` returned nothing and the resolver handed back `2620:12a:8000::4` and `2a00:1450:4003:80b::200e` respectively — both blocked-list domains reached the network unimpeded. After additionally emitting `::1 <domain>` for each blocked domain (82 entries: v4=41 v6=41), both domains now resolve to `0.0.0.0` and the block holds. The `mod_network_layer1_harden` generator now emits the IPv4 and IPv6 sinkholes together per domain, deterministically; the file is regenerated from the blocklists each apply so the pairs cannot drift, and `127.0.0.1 localhost` remains the only baseline entry. |
| F14g | `/etc/hosts` blocking is bypassed by connmand's DNS proxy on 127.0.0.1:53 (libc `getent` honours the file; webOS daemons + `nslookup`/BusyBox resolution do NOT) | `network` (layer 4) | **Full** for the device's libc + daemon resolution paths | **The proof.** On this device, `getent hosts ngfts.nextlgsdp.com` returns `::1` (the sinkhole) but `nslookup ngfts.nextlgsdp.com 127.0.0.1` returns the real IP `23.211.135.15` — and the TV *connected* to it. `/etc/hosts` cannot express subdomains entry-by-entry: blocking `nextlgsdp.com` did nothing for `es.nextlgsdp.com`. Observed live: `es.nextlgsdp.com`, `ES.ibsstat.nextlgsdp.com`, `eic.cdpbeacon*.lgtvcommon.com`, `eic.lgchhomeapp.lgtvcommon.com`, `eic.cdplauncher/cdpsvc.lgtvcommon.com`, `eic.ads.lgtvcommon.com`, `eic-ngfts.lge.com`, `cf-kic/EIC.lggalleryplus.com`, `static.doubleclick.net`, `www.googletagmanager.com` all kept resolving to real IPs after layer 1. **The fix (layer 4).** `scripts/dnssink.py` is a pure-stdlib DNS sinkhole resolver that listens on `127.0.0.2:53` (UDP + TCP) and answers queries for blocklisted suffixes (one `nextlgsdp.com` entry covers `es.nextlgsdp.com` and `ES.ibsstat.nextlgsdp.com`) with `0.0.0.0` / `::` / NXDOMAIN; everything else is forwarded to the upstream resolver discovered via `connmanctl` → LS2 `com.webos.service.connectionmanager/getStatus` → default gateway, and the response is relayed byte-for-byte (transaction ID preserved). Blocklist source: `/var/lib/own-your-glass/hosts` — the same file layer 1 generates; mtime-watched, so `oyg harden --only network` after editing blocklists picks them up without a restart. Auditing: every blocked lookup is logged to `$OYG_ROOT/dns-audit.log` (timestamp, client IP, name, qtype) — that audit trail IS the proof that layer 4 is doing real work. **How ConnMan is told (Variant 1, no connmand restart).** `scripts/dns.sh` finds the active ConnMan service settings file (`/var/lib/connman/<svc>/settings`), backs it up to `$OYG_BACKUP/connman-…orig` (once, preserved forever), appends `Nameservers=127.0.0.2;` to the live file, and nudges connmand via `kill -HUP <pid>` (the documented ConnMan reload signal). We never run `systemctl restart connman`: that can drop the Wi-Fi IP and there is no telnet lifeline (webOSbrew `telnetd` is disabled by the `policy` module). If HUP doesn't make connmand pick up the new file (config scan on next DHCP or other event), `dns.sh` would rather the TV be offline than restart it. **Connection to A-sinkhole semantics.** A blocked `A` query returns `0.0.0.0` and the kernel then refuses the connection before any packet egresses — the same outcome `/etc/hosts` would give if libc-resolving daemons had honoured it. A blocked `AAAA` returns `::` (IPv6 refusal). Any other qtype returns NXDOMAIN. **Watchdog.** `scripts/watch-dns.sh` probes every 15 s; if the resolver dies and does not come back after 3 restarts, the watchdog restores ConnMan's original settings from the backup so the TV is never left without DNS. **Operator opt-in.** `OYG_DNS_RESOLVER=1 oyg harden --only network` makes layer 4 active. `oyg status` shows resolver live/upstream/etc. Boot hook re-applies on every boot when state has `network.dns_resolver=1`. **Pure stdlib** — no `dnslib` / `twisted`; matches `scripts/sniff.py`'s convention. **Verified on this device.** Below. |
| F14e | `/usr` is read-only but `mount --bind /dev/null` works | `capture` (vtCaptureTestSuite neutralisation) | **Full** | `chmod 000` against `/usr/bin/vtCaptureTestSuite` fails because `/usr` is read-only. `mount --bind /dev/null /usr/bin/vtCaptureTestSuite` works (verified) and replaces the binary with the null device for execution purposes. `restore` umounts the recorded path. |
| F31 | Chromium told to trust `http://doklo.lge.com` and `http://wam.lge.com` as secure | (out of scope) | **Not possible** | That trust list is baked into the vendor Chromium binary on the read-only `/usr` partition. Cannot be edited without modifying firmware. |
| F33 | LG cloud control plane (AWS IoT MQTT) | `services` (`iot-client`, aggressive) | **Partial** | The only lever is `OYG_AGGRESSIVE=1` to also stop `iot-client` and `pushclient` (and have the boot hook re-stop them). This BREAKS ThinQ (the owner's phone app, voice control, push notifications). Documented in README warn-on-risk. |
| F34 | ThinQ edge rules engine | `services` (`pushclient`) | **Partial** | Same as F33. |
| F36 | `iot-client` long-lived MQTT session | `services` (`iot-client`) | **Partial** | Same. |
| F33a | `iot-client` is a **luna-launched** LS2 service, NOT a systemd-managed process | `services` (kill step + per-spec format) | **Full** (with the caveat that ls-hubd may respawn it on demand) | Verified on device: `/usr/palm/services/com.webos.service.iotclient/iot-client` is the long-running process (PID 4497 in this observation); its systemd unit `iot-client.service` only runs `/etc/systemd/system/scripts/iot-client.sh` and exits 0 (`Active: inactive (dead)`, `code=exited status=0`). `systemctl stop iot-client` and `systemctl is-active iot-client` are therefore misleading: the unit says `inactive` while the process is running. `pidof iot-client` returns the live PID; `kill <pid>` works and nothing respawned within 25 s. The module's spec format is `<id>\|<unit>\|<process>` so each target can have a different unit and process name (pushclient's real unit is `com.webos.service.pushclient.service`; its process name happens to be `com.webos.service.pushclient`). The kill step is re-applied on every boot via the init.d hook reading `services.kill`, because `ls-hubd` may respawn the process if something requests the LS2 service — this is best-effort control, not permanent disable. |
| F34a | `pushclient` real unit name is `com.webos.service.pushclient.service`, not `pushclient` | `services` (per-spec format) | **Full** | The module's old format (bare service name) could not find this unit; the spec format `<id>\|<unit>\|<process>` carries the full unit name explicitly so this works. |
| F37 | Magic Remote microphone via Bluetooth HID (`/dev/hidraw0`, HID_NAME=`LGE MR25GA`) | `voice` | **Full** | The Magic Remote mic audio arrives on `/dev/hidraw0` as a raw HID stream consumed by `voiceinput_hidraw`, which feeds `voiceinput` then `voiceconductor`. Only `voiceinput_hidraw` reads `/dev/hidraw0`; no ALSA device is involved. Verified on device: after the `voice` module is applied, all three processes are GONE and do not respawn within 10 s. The remote's BUTTONS still work — input devices `LGE RCU`, `LGE M-RCU - Builtin [0..2]`, `LGE Simple Premium` remain functional, and the mic button press still registers as `NL_BUTTON_CLICK {"button_type":"KEY_VOICE"}` in `/tmp/var/log/messages`. Audio playback is unaffected (`paplay rc=0`). Controlled test (log watcher paused, both `/tmp/var/log/messages` and `/tmp/app.voice.log` wiped to 0 bytes, mic button pressed twice): `/tmp/app.voice.log` stayed at 0 bytes; `/tmp/var/log/messages` recorded only the single `NL_BUTTON_CLICK` event — ZERO `user_utterance`, ZERO voice `NL_*` events. **The HID device `/dev/hidraw0` itself is deliberately untouched**: the mic button press rides the same HID channel as every other remote button, and bind-mounting `/dev/null` over `/dev/hidraw0` would break the remote. We neutralise the consumer processes instead (`chmod 000` + `mount --bind /dev/null` over each binary, then `pidof`+`TERM`+`KILL`). This module is in the boot hook because `mount --bind` does not survive a reboot. |
| F38 | LG consents in `/var/luna/preferences/eula` flip back to `"accepted":true` without user action | `policy` | **Full** (default decline-all); **Full** (allow-list, scoped to named ids) | Six entries are `"accepted":true` on this device (`S_DPA`, `S_SVC`, `S_VNG`, `S_ADG`, `S_TAG`, `S_MKT`); earlier in this investigation all six were `false`, and they re-flip to `true` when a related component updates or an account is re-bound. The `policy` module backs the file up once to `$OYG_BACKUP/eula.orig` (never overwritten) and rewrites every `"accepted":true` to `"accepted":false` using `sed` written to a temp file then `mv`'d (no `python` on the device; pattern is unambiguous in this file). After every rewrite a JSON sanity check (`{` first, `}` last, both non-whitespace-trimmed) catches a pathological sed and rolls back from backup. Default is decline-all; allow-list form `OYG_TOS_IDS="S_VNG S_TAG"` declines only the named ids, implemented as per-id sed patterns safe because `"id"` immediately precedes `"accepted"` in every entry on this device. State records `before`, `after`, `flipped` so `status` can report what changed on the last run. **The state must be re-enforced at every boot** — that is why the boot hook re-runs `oyg harden --only policy` alongside `capture`/`mic`/`voice`/`logs`/`remoteone`. **Warning**: declining some entries (`S_SVC`, `S_DPA`, `S_MKT`) may disable LG service / smart-home features. This is intentional. |
| F38a | Force-declining `S_VNG` does NOT uninstall the ACR engine | `policy` + `services` + `mic`/`voice` + `network` | **Full in combination; Partial alone** | `S_VNG` is the *Viewing Information Gathering* consent — the ACR consent. Declining it removes the vendor's *legal cover*, which is the point of this finding. But the ACR **engine** itself is still installed (`/usr/bin/acr` etc.) and the `acr` systemd unit is still present. What actually neutralises the engine is the combination: (a) `services` stops + boot-enforces `acr` + `contentminer` + `objectdetection`; (b) `mic` neutralises every ALSA capture PCM so audio cannot reach ACR's consumer; (c) `voice` neutralises the Magic Remote mic pipeline (`voiceinput_hidraw` → `voiceinput` → `voiceconductor`) so no audio reaches the engine from the Magic Remote path either; (d) `network` sinkholes the ACR cloud endpoints via `/etc/hosts` (dual-stack, so AAAA lookups cannot fall through to DNS — see F14f). The `policy` module's force-decline of `S_VNG` is the consent-layer defence that complements the engine-layer (`services`), audio-layer (`mic`+`voice`), and network-layer (`network`) defences. None of them alone is sufficient. |
| F38b | LG consent state lives in **four** on-disk stores; pre-`policy`-C the module patched only one. The SDX/ACR delivery pipeline consults the OTHER three | `policy` (Jobs B + C + D) | **Full** (post-`policy`-C); **Partial** (pre-`policy`-C) | Three stores are silently present alongside `/var/luna/preferences/eula` (which Job B handles): `/mnt/lg/cmn_data/sdp/eula-service/eula.json`, `/mnt/lg/cache/sdp/eula-service/eula.json`, `/mnt/lg/user/sdp/eula-service/eula.json`. All three are tiny JSON files of the same shape: `{"statusList":[{"managementTypeCode":"S_DPA","status":"A",...},{"managementTypeCode":"S_SVC","status":"A",...},{"managementTypeCode":"S_VNG","status":"A","versionId":"..."},{"managementTypeCode":"S_MKT","status":"A",...},{"managementTypeCode":"S_ADG","status":"A",...},{"managementTypeCode":"S_TAG","status":"A",...}],"synchronized":true}`. Codes: `S_VNG` = viewing-info/ACR, `S_MKT` = marketing, `S_ADG` = ad targeting, `S_TAG` = analytics. Verified on this device: **all six are `"A"` (Accepted) in all three stores**. Pre-`policy`-C the `policy` module only patched `/var/luna/preferences/eula` (the Settings-UI-facing view), so the SDX/ACR path kept reading `A` for every code — `policy` Job B was solving one quarter of the consent-store problem. **The `policy` module's Job C** now backs each of the three stores up ONCE to `$OYG_BACKUP/eula-<store>.orig` (cmn/cache/user; never overwritten) and rewrites every `statusList[].status` `"A"` to `"D"` using `python3` (guarded — warn + skip if absent rather than corrupting; write to a temp file then `mv` atomically). Pre-existing JSON fields are preserved byte-for-byte (key order is reordered by python's `json.dump` to its canonical-separator set, which the consumer treats identically). Idempotent: a second run does not re-back-up over a good backup nor double-edit; status reports per-store codes (e.g. `policy/sdx.cmn: S_DPA=D S_SVC=D ...`) and FAILs if any store still has `"A"`. Skip-if-absent: any missing store is reported N/A and does not fail the module. The stores are rewritable by `eulaservice`/SDX on their own, so Job C is re-applied on every boot by the existing `policy` entry in the boot hook's `harden --only` list — extending the module was sufficient; no install.sh / boot-hook change was required. Restore (`oyg restore --only policy`) puts the backups back, returning each store to its pre-own-your-glass state. |
| F40 | Tier A unused feature services (mycar, familycare, buddyconnector, alwaysready, ai-inference-manager, avahi-{daemon,adaptor}, com.webos.service.ruleengine) and preloaded voice-app UI (~51 MB) still resident | `debloat` (opt-in `OYG_DEBLOAT=1`) | **Full** | Opt-in (default off — these are consumer features). Each Tier A unit's backend is already blocked or neutralised by other modules: ThinQ cloud is sinkholed by `network` (F14/F14f), the mic and voice pipelines are dead (`voice` + `mic`), the ACR engine is stopped + consent-declined (`services` + `policy`). `alwaysready` is a wake-word listener — with no live mic pipeline it can never fire. So disabling these front-end processes reclaims RAM + attack surface with zero loss of functionality. The `com.webos.service.ruleengine` ThinQ edge-rules daemon's cloud control plane is already blocked by `network`. `com.webos.app.voice` is NOT a systemd unit — it is a luna-launched preloaded app (binary `/usr/bin/com.webos.app.voice`, ~51 MB) — neutralised with `chmod 000` + `mount --bind /dev/null` (the proven technique that works on read-only `/usr`), then `kill`ed by basename. `com.webos.service.familycare` runs under `iotjs` (`/usr/bin/iotjs .../familycare/index.js`), so `pidof familycare` never matches; per the brief we rely on `systemctl stop` only and treat a missing process as normal. Luna-launched binaries with NO systemd unit (handled by binary neutralisation, skipping gracefully if absent): `/usr/sbin/lg.thinqai.adapter`, `/usr/sbin/airessrvallocator`, `/usr/sbin/com.webos.service.iotproxy`, `/usr/sbin/sportsalert`. Enforcement is the same model as `services`: `systemctl mask` is impossible on this device (read-only `/etc` — see F14a), so the model is "stop now + stop again on every boot". Persisted under `$OYG_ROOT`: `debloat.units.stopped` (unit names to stop at every boot) and `debloat.procs.kill` (process names to kill at every boot); the boot hook re-applies both when state contains `debloat.applied=1`. Restore `umount`s + chmod-restores exactly what was bound, and `systemctl start`s previously-active units; it does NOT restart killed processes (luna-launched / preloaded — they respawn on demand if requested). The module is deliberately narrow and never touches `surface-manager`, `WebAppMgr`, `flutter-client` (home UI), `com.webos.app.inputcommon`, `legacy-broadcast-dvb` (tuner), audio services, `connman`, `bootd`, `db8-*`, `lginput2`, `memchute`, `crashd`, `configd`, `micomservice`, `fancontroller`, `faultmanager`, `tvpower`, `lowlevelstorage`, `ls-hubd`, `luna-*`. The home launcher keeps rendering; the remote keeps working; the TV keeps waking on HDMI and turning off on schedule. Fully reversible with `oyg restore --only debloat`. |
| F40b | Tier B luna-launched binaries burning CPU / attack surface — DIAL discovery server `ss.gateway` (CPU 6m09s and climbing, TCP 8008), phone-connectivity helper `iconnectivity` | `debloat` (opt-in `OYG_DEBLOAT=1`) | **Full** | Same opt-in model as F40 (default off — these are consumer features). Both are luna-launched, no systemd unit. The binary spec format supports an optional kill-name override (`<path>\|<proc>`); used for `ss.gateway` because the file is `discovery-server.js` but `argv[0]` is set to `ss.gateway` by the node launcher. Bind target is always the file path; the override only affects the kill step. Each is neutralised with `chmod 000` + `mount --bind /dev/null` (works on read-only `/usr`), then killed by the override name. **What it costs:** `ss.gateway` — kills DIAL second-screen / casting discovery (Chromecast-style "cast to TV"); `iconnectivity` — kills LG phone-pairing / TV Companion. **Measured:** `MemAvailable` 471 MB → **868 MB** (+397 MB, ~84% gain), `MemFree` 128 MB → **196 MB** (+68 MB). After applying, `ss.gateway` is gone and does not respawn (rebind via the boot hook catches it if it does). Restore `umount`s + chmod-restores each path exactly; the boot hook re-applies on every reboot when state contains `debloat.applied=1`. The third candidate of this Tier B set — LG's Service Delivery eXtension `sdx` (`/usr/sbin/sdx`, LS2 service `com.webos.service.sdx`) — looked like pure vendor surface but turns out to be **load-bearing for the TV's own Settings UI**: bind-neutralising it silently breaks the gear/Settings button with no user-visible error. It is therefore deliberately excluded from `debloat` and documented separately as F42. |
| F40c | `uploadd` log-upload daemon survives `systemctl stop` + `kill` because its LS2 service is `Type=dynamic` | `debloat` (opt-in `OYG_DEBLOAT=1`, bind-class) | **Full** (structurally — bind defeats respawn) | Verified: `/usr/share/luna-service2/services.d/com.palm.uploadd.service` has `Type=dynamic`. `ls-hubd` spawns `/usr/sbin/uploadd -v` on the next call to `luna://com.palm.uploadd/...`. The boot hook stops the systemd unit and kills the process, but the next caller brings it back (observed pid 11956, parent=`ls-hubd`). The structural fix is to remove `uploadd` from the unit-class spec and add `/usr/sbin/uploadd\|uploadd` to `DEBLOAT_BINARIES_SPEC`; the spec parser uses the basename by default so the explicit `\|uploadd` suffix is symmetric with the `ss.gateway` entry. `chmod 000` then `mount --bind /dev/null` over the binary; even when `ls-hubd` re-execs the path, the new process opens `/dev/null` and exits immediately (verified: `pidof uploadd` stays empty and a deliberate `luna-send -i -n 1 -f luna://com.palm.uploadd/upload '{}' </dev/null` returns `{"serviceName":"com.palm.uploadd","returnValue":false,"errorText":"com.palm.uploadd is not running."}` with the PID still empty). Bind does not survive a reboot — the boot hook re-applies the bind every boot when `debloat.applied=1`. `wowplay` is `Type=static` and is therefore handled in `DEBLOAT_UNITS_SPEC`, not here. |
| F41 | Launcher renders unwanted apps (LG ad machinery, SDK examples, demo apps, RemoteOne front-end tile) | `apps` | **Full** | The supported on-device mechanism is LG's own `blockedSystemAppList/<REGION>.json` (`<REGION>` is the device's region code, e.g. `ESP` in Spain). The file is a small JSON object: `{"blocked_system_applist":["com.webos.app.buddy",...]}`. `/var/preferences` is writable on this device. Apps whose IDs appear in the array are HIDDEN from the launcher (not deleted — the binaries stay on disk and can be re-shown by removing their IDs). The module discovers the file dynamically (glob `blockedSystemAppList/*.json`, prefer any existing one) so it does not hardcode the region. It backs up the original ONCE to `$OYG_BACKUP/blockedSystemAppList.<REGION>.json` (never overwritten) and writes via temp file + `mv` (no in-place edit). Implementation uses only `awk`/`sed`/`tr`/`sort`/`uniq` — no `python` on the device. Curated list of 48 IDs in four groups (Ad/ACR, remote-support, SDK examples, demo/test). **Skip-if-absent**: each curated ID is checked against `/usr/palm/applications/<id>` and `/media/system/apps/<id>` before being added; absent IDs are listed in a warning so the module is portable across webOS builds. Status reports `N/M curated present`. The boot hook re-runs `oyg harden --only apps` on every boot because the application manager may rewrite the file on its own (related component updates). **What it does NOT do**: store-delivered promotional tiles such as "Rakuten TV" or "LG Streaming Week" are not local apps — they are pulled from the content store / recommendation feed. Hiding a local `com.webos.app.*` ID will not touch them; the network blocklist (`network` module's `/etc/hosts` bind-mount + resolver blackholes) is what reduces that class. Fully reversible with `oyg restore --only apps`. |
| F42 | LG's "Service Delivery eXtension" (`com.webos.service.sdx`, `/usr/sbin/sdx`, `Type=dynamic` LS2) **cannot be neutralised** — doing so silently breaks the TV's Settings UI | (no module — left running by design); `network` (endpoint coverage as the alternative mitigation) | **N/A** for the binary (deliberate non-intervention); **Full** for endpoint coverage | What SDX is. LS2 service spawned on demand by `ls-hubd`. From its own strings: downloads the **user agreements / EULA** ("FirstUse mode, sdx tries to download user agreements by it-self", `requestEulaDownload`), downloads the **ACR file** (`/tmp/acr.xml` — `ACR_On="true" send_data="true" ACRServiceLaunched="true" capture_method="SOURCE" capture_format="YUV420" ACRSolution="ALPHONSO" CountryCode="ES" server_address=""`; `Can NOT download ACR file, HTTP:`, `ACR download is failed`), downloads `smartConfig`, performs **device authentication** (stamping an encrypted `X-Device-ID`, `X-Device-Type: T01`, `X-Device-Product: webOSTv 25`, `X-Device-SDK-VERSION: 10.3.1` on every service request via `luna://com.webos.service.sdx/getHttpHeaderForServiceRequest`), and resolves the LG server endpoints via `/mnt/lg/cmn_data/sdp/sdx/server_addr_version.conf` (14 entries). The ACR config delivery is the same binary that the original investigation set out to stop — i.e. the thing the investigation was about and the thing that breaks the Settings button are the same binary. **The failure mode** (LG OLED55B56LA / webOS 10.3.1). `debloat` used to `mount --bind /dev/null /usr/sbin/sdx`. With that in place, pressing the remote's **gear/Settings** button does NOTHING AT ALL — no error, no spinner, no dialog — and the launcher's gear icon fails identically (both the remote key and the launcher route through `com.webos.app.quicksettings`, launched via `com.webos.service.systemUIManager`). `surface-manager` still logs `com.webos.app.quicksettings visible:true`, so the system log looks perfectly healthy. The only reliable tell is a line that is *never* emitted: `com.webos.app.quicksettings NL_QUICKSETTINGS_EDITMODE {...}`. Restoring the binary makes the panel work immediately. `/usr/sbin/sdx` is therefore deliberately **excluded from `DEBLOAT_BINARIES_SPEC`** with a "DO NOT add back" note in `modules/debloat.sh`. **THE PASS/FAIL SIGNAL** (the reusable lesson — document it first): after a gear press, `grep QUICKSETTINGS_EDITMODE /tmp/var/log/messages`. Present = the panel initialised. Absent = it did not, regardless of what `visible:true` says. This holds for any change that touches the SDX binary or the `com.webos.app.quicksettings` launch path. **Generalised lesson**: a "luna-launched binary" is NOT necessarily an optional front-end. Before neutralising one, confirm the TV's own UI still works — especially the settings/quick-settings panel, which fails with no user-visible error path. **Methodology trap** (this is what cost most of the debugging time): a live `oyg restore` cannot undo a bind the *boot hook* applied, so the effect only ever appears at boot — live A/B tests contradict each other and produce false conclusions. Reproduce the failure with a full reboot, not a live `umount` + re-bind cycle. **Endpoint coverage as the alternative.** SDX keeps its state in `/mnt/lg/cmn_data/sdp/sdx/`; `server_addr_version.conf` declares 14 endpoints. Checked against everything shipped before this investigation, only **2 of 14** were covered by any list (the upstream `etc/blocklist-upstream-safe.txt` happens to cover `lgtvsdp.com` and one regional `nextlgsdp.com` entry). All 12 missing ones were added to `etc/blocklist-oyg.txt`, tiered: **SAFE** — `rdx2.nextlgsdp.com` (sdx rdx_secure — remote diagnostics + device-log upload), `ibs.nextlgsdp.com` (sdx ibis_secure — IBIS telemetry), `ibsstat.nextlgsdp.com` (sdx ibis_stat_secure — IBIS statistics), `nudge.lgtvcommon.com` (sdx nudge_secure / nudge_log_secure), `pnv.lgtvcommon.com` (sdx cpv_secure — CSI content/viewing endpoint), `qcardservice.lgtvcommon.com` (sdx qcard — home-screen promo-card delivery), `lggalleryplus.com` (sdx lg_gallery_plus — LG Gallery+ content delivery). **STRICT** — `nextlgsdp.com` (the SDP core: sdp_auth / init / common / apps / onnow / search / nlp / airplay — i.e. the LG app store + app-update delivery), `tv.wiselg.com` (voice proxy, IoT push, sports, CDP, LG shop, account, buddy), `api.lgtviot.com` (ThinQ IoT cloud v3.0), `wiseconfig.lgtvcommon.com` (WISE smartConfig delivery), `wiseresource.lgtvcommon.com` (WISE resource delivery). The STRICT trade-off is real and worth stating: `nextlgsdp.com` is the app-store/app-update delivery platform, so STRICT mode gives that up — installed apps still launch, but new installs and updates stop. **Net coverage delta on this device**: 63 → 75 blocked domains (126 → 150 entries in the generated hosts file, every domain emitted dual-stack `0.0.0.0`+`::1` per F14f, plus the `127.0.0.1 localhost` baseline). SDX keeps running but every endpoint it knows about is now unreachable from the libc-resolving daemons that hit it, and its `X-Device-ID` is now stamped onto requests that have nowhere to go. |
| F43 | LG `marketingAllowedDate.json` schedules a 2-year toast re-prompting the user to re-allow marketing; the `policy` module's Job D moves the file aside so the callback cannot fire | `policy` (Job D) | **Full** | File: `/mnt/lg/cmn_data/sdp/eula-service/marketingAllowedDate.json` (427 bytes). Schema: `{"activitymanager":{"start":true,"activity":{"callback":{"method":"luna://com.webos.service.eulaservice/marketingAllowedCallback","params":{"type":"create_marketing_notice"}},"description":"Create toast within 2 years that reminds marketingAllowed","schedule":{"start":"2028-04-13 19:42:01","local":true},"type":{"foreground":true,"persist":true}}},"YYYY":"2026","MM":"07","DD":"13","count":0}`. On the scheduled start the activity-manager fires the `marketingAllowedCallback` LS2 method, which causes `com.webos.service.eulaservice` to raise a toast asking the user to re-allow marketing. **What we do.** Job D backs the file up once to `$OYG_BACKUP/marketingAllowedDate.json.orig` (preserved forever) and renames the live file to `<path>.neutralised`. Activity-manager cannot find a missing schedule file, so the callback never fires. **Why not delete?** Restore is trivial: `oyg restore --only policy` `mv`s the neutralised file back over the live path. **Skip-if-absent.** If `eula-service` recreated the file with a new schedule between boot cycles, the restore still puts our captured (older) schedule back — that is the desired behaviour because we want to undo the agent's own state. Status reports `OK` when the live path is gone and the neutralised path exists, `N/A` when neither exists, and `FAIL` when the live path is back (the boot hook will move it aside again on the next cycle). Idempotent: a second harden reports "already neutralised" and does nothing. |
| F43a | The status code `"D"` written by `policy` Job C is the conventional counterpart to `"A"` but its exact meaning is **UNVERIFIED** on this device | `policy` (Job C) — caveat applies | **Partial** (honesty disclosure) | The `statusList[]` in the SDX-side eula stores uses a one-character status code. `"A"` is observed for every code on a fresh device (Accepted, all six). `"D"` is what the Settings UI emits when the user toggles an entry off, so it is the conventional counterpart. **What we do not know.** `/usr/sbin/eula-service` exposes no enumerating string, no `luna://.../listStatusCodes` method, and the only error line in its log is `"Unknown EULA status"` — so we cannot confirm `"D"` maps to a specific code path on this firmware. The official "force all declined" lever is `luna://com.webos.service.eulaservice/resetEula`, which we deliberately do NOT call: its documented behaviour is "The TV will now restart to show the updated User Agreements" — i.e. it forces a restart into the User Agreements wall, which is the opposite of what we want. **Accepted risk.** A wrong status code may cause the TV to re-prompt for the User Agreements wall. This is reversible (the per-store backup at `$OYG_BACKUP/eula-<store>.orig` is on disk; `oyg restore --only policy` puts the original `"A"` values back). **Why the trade-off is still worth it.** The current state — every code `"A"` in every SDX-side store — leaves the SDX/ACR/QCARD pipeline with full legal cover. The `"A"` → `"D"` rewrite at least pushes the TV's consent state in the documented direction; an "I cannot tell what happens" but I can still revert it is better than the device's confirmed default of "everything accepted and reported to LG cloud". Verified on this device: after rewrite, `python3 -c "import json; d=json.load(open('/mnt/lg/cmn_data/sdp/eula-service/eula.json')); print([s['status'] for s in d['statusList']])"` shows `['D','D','D','D','D','D']`, no User Agreements wall has appeared, and `oyg status` reports `OK policy/sdx.cmn: S_DPA=D S_SVC=D S_VNG=D S_MKT=D S_ADG=D S_TAG=D`. If the wall ever does appear, `oyg restore --only policy` returns the device to its pre-own-your-glass state in one command. |
